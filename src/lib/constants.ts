export const APP_NAME = "Habit Quest";

export const DEFAULT_FREEZE_COUNT = 1;

export const HABIT_RULE_TYPES = [
  "daily",
  "every_x_days",
  "weekly",
  "selected_weekdays",
  "custom",
] as const;
